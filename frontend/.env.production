# Backend URL on Railway. Override at build time in the Vercel dashboard:
#   VITE_API_URL = https://<your-railway-service>.up.railway.app
# This must be set in Vercel for production. The SPA rewrite only handles
# client-side routes; it does not proxy API requests.
VITE_API_URL=
