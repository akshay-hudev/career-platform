# Backend URL on Railway. Override at build time in the Vercel dashboard:
#   VITE_API_URL = https://<your-railway-service>.up.railway.app
# Leave empty to use the vercel.json rewrite (/api/* → backend).
VITE_API_URL=